travis.yml

at first just collect the output and then write it to a file
  - chain with entr or when-changed to perform an arbitrary action
next run an arbitrary command over the output (avoid the need of entr or when-changed)

output for less than 3 seconds then ^D and it breaks :-/
